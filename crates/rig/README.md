# Rig

Umbrella crate for Rig core APIs, providers, and integrations.

By default this crate re-exports `rig-core`. Optional integration crates are
available behind feature flags such as `mongodb`, `qdrant`, `lancedb`, and
`bedrock`.
